{
  "name": "hazard-pay-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HAZARD PAY — the renderer. Three.js, first person, predicts nothing it is not allowed to.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.19.3",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}

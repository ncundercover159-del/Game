{
  "name": "rerun-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "build:solo": "vite build --config vite.solo.config.js && node ../artifact/build.js",
    "build:2d": "vite build --config vite.2d.config.js && node ../../rerun2d/build.js"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
